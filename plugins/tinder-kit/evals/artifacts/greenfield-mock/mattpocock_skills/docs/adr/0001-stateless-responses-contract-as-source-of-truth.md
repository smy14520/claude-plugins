# 0001 — 响应完全无状态，契约是唯一真值来源

mock-server 的每个 HTTP 响应都严格由 `mocks.json`（契约）静态决定，服务器不保存、不记忆任何请求历史。曾认真考虑过有状态模拟（如 `POST /api/login` 成功后 `GET /api/user` 返回登录态），被否决：有状态需要内存状态模型、重置语义与并发处理，会把一个轻量联调工具变成小型应用服务器；而无状态让契约同时是一份可读文档。已知代价：演示「登录后」这类连续场景时，必须在契约里直接写死登录后的数据。
