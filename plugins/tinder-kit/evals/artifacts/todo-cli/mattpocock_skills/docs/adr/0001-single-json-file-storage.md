# 0001：用单一 JSON 文件持久化，不用 SQLite

Todo 数据需要本地持久化。我们决定把全部数据放进用户主目录下的一个 JSON 文件（`~/.todos.json`），并显式拒绝 SQLite 或任何数据库。理由：单人工具的数据量小到查询性能无关紧要；JSON 零依赖、肉眼可读、能直接被 grep/jq 或纳入版本库；不引入存储引擎就没有安装与升级负担。

代价是并发与事务安全需自行负责——写入走"临时文件 + `os.replace` 原子改名"；读到的文件若不是合法 JSON 则报错退出、绝不写回，宁可拒绝对付也不冒覆盖数据的风险。
