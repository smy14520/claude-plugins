# 08 — 旧数据迁移

Type: task
Status: needs-triage
Blocked by: 02, 03

## Question

现存 `.todos.json`（在团队成员某台机器上，需人类拿到文件后提供）迁入新平台：

1. 人类 checklist：取回现存 `.todos.json` 文件，确认其字段结构与规模（多少条、有无 done 历史）
2. 写一次性迁移脚本：旧模型（id/title/done）映射到「任务领域模型」敲定后的新 schema，落入「存储选型」敲定后的存储
3. 迁移后旧 CLI（todo.py）废弃——保留于 git 历史即可

Answer 需记录：迁移了多少条数据、字段映射表、脚本位置。
