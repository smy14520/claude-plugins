# 09 — 部署形态与数据备份

Type: grilling
Status: needs-triage
Blocked by: 03

## Question

「单台机器 + Docker」已定，细化到可执行：

- **容器形态**：单容器（app 内含定时任务）还是 app + db 两容器（docker-compose）？
- **数据卷与备份**：数据放哪个卷？备份策略（定时打包 + 保留几份？备到哪里）？无专职运维意味着备份必须全自动。
- **入口**：HTTPS 用 Caddy 自动证书，还是内网裸 HTTP 先跑？访问范围是全公司还是仅团队网段？
