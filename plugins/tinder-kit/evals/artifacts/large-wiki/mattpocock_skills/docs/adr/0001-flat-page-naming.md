# 页面采用全 Vault 扁平命名，子目录不参与链接解析

个人 Wiki 中链接应随文件在目录间的移动而保持有效，因此页面身份 = 文件名去掉 `.md` 后缀，Vault 内唯一；子目录纯粹是整理手段，不构成命名空间，链接 `[[foo]]` 可指向任意深度的 `foo.md`。选此方案而非路径命名（`[[projects/foo]]`）是因为路径命名会使链接随文件移动而断裂，且与 Obsidian 的 wikilink 习惯不兼容、迁移成本高。

后果：Vault 内出现同名文件不再可解析为单一目标，属于**缺陷**，由体检报告暴露，而不是由解析器静默择一。
