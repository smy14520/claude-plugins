# Legacy pointer

Use `.arbor/tasks/<package>/prd.md` as the active brainstorm output. Brainstorm finalize records one ready package through `sdd-arbor finalize-brainstorm`; large scopes use PRD-local `## Slices`. This file is kept only for old references.
