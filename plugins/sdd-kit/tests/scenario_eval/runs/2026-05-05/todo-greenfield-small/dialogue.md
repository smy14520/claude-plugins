# Scenario dialogue — todo-greenfield-small

- 时间：2026-05-05T08:27:57.512217+00:00
- Profile：greenfield-small
- Model：claude-opus-4-6
- AI user model：claude-opus-4-6


## 初始用户需求

我想从零做一个本地优先的轻量 Todo Web 应用，面向个人每天管理少量任务，核心只需要快速新增、标记完成、按“今天/稍后/已完成”分组，以及查看当天完成记录。

## Brainstorm Turn 1

### 被测 agent

我已创建 brainstorm draft：`.arbor/tasks/05-05-local-first-todo-web-app/prd.md`，并把你已有的需求写入了初始 PRD。

当前理解：这是一个从零开始的新项目，首版目标是个人本地使用的轻量 Todo Web 应用，核心闭环是：

- 快速新增任务
- 按“今天 / 稍后 / 已完成”分组
- 标记完成
- 查看当天完成记录
- 本地保存数据，不依赖远端服务

第一个最高价值问题是：**你希望“本地优先”具体落成哪种运行与持久化形态？**

我推荐：**纯浏览器静态应用**。  
理由：最轻、最符合“个人每天少量任务”和“快速打开即用”；用 `localStorage` 或 `IndexedDB` 保存任务，不需要账号、后端、数据库或部署复杂度。缺点是首版不做跨设备同步。

你可以选一个：

1. **纯浏览器静态应用（推荐）**：前端本地存储，打开网页即可用。
2. **PWA 离线优先**：可安装、离线缓存，体验更像 App，但首版复杂度稍高。
3. **本地后端 + SQLite**：数据更可控，适合未来导出/脚本，但运行门槛更高。
4. **先不定技术**：先聊产品语义，但这会阻塞最终 PRD 的技术边界。

### AI user

我选 **1. 纯浏览器静态应用**。

首版我希望它就是打开一个本地网页或静态站点就能用，数据保存在浏览器本地，不需要账号、后端、数据库或同步服务。这个产品的重点是“每天快速记几件事、当天清掉”，不是长期项目管理，所以跨设备同步首版可以不做。

持久化上我只关心两点：刷新页面和关闭浏览器后任务不能丢；同一台电脑同一个浏览器里能持续使用。localStorage 或 IndexedDB 由你们按实现合适性选择即可。

## Brainstorm Turn 2

### 被测 agent

已更新 PRD draft：确认首版是**纯浏览器静态应用**，数据只保存在浏览器本地；刷新、关闭浏览器后同一台电脑同一浏览器内数据不能丢；账号、后端、数据库、同步服务、跨设备同步都明确排除在首版外。

下一个最高价值问题：**“今天 / 稍后”之间的任务流转规则应该是什么？**

我推荐：**默认新增到“今天”，手动移到“稍后”，稍后任务不会自动回到今天。**  
理由：最符合轻量产品定位，避免引入日期计划、自动滚动、提醒等复杂规则；用户每天只需要手动挑今天要做的几件事。

请选择一个：

1. **手动流转（推荐）**  
   新任务默认进“今天”；用户可手动把任务移到“稍后”；稍后任务只在用户手动选择时回到“今天”。

2. **新增时选择分组**  
   新增任务时可以直接选择“今天”或“稍后”；更灵活，但新增动作会多一步或多一个控件。

3. **稍后自动滚入今天**  
   稍后任务可以设置日期，到期自动进入今天；更像日程工具，复杂度明显增加。

4. **只有今天，稍后只是折叠区**  
   所有未完成任务本质相同，“稍后”只是视觉上暂存/折叠，不承担明确计划状态。

### AI user

我选 **1. 手动流转**。

新任务默认进入“今天”，因为这个工具主要服务快速捕捉当天要做的事；如果我发现它不是今天必须处理的，就手动移到“稍后”。

“稍后”不要自动回到“今天”，也不需要日期、提醒或到期规则。每天早上或开始工作时，我会自己从“稍后”里挑几件移回“今天”。这能保持产品简单，也避免它变成日历或项目管理工具。

## Brainstorm Turn 3

### 被测 agent

已更新 PRD：确认“今天 / 稍后”是**手动流转**。

当前已定规则：

- 新任务默认进入“今天”
- 未完成任务可以手动从“今天”移到“稍后”
- “稍后”任务可以手动移回“今天”
- “稍后”不会自动回到“今天”
- 首版不做日期、提醒、到期、日历或自动滚动规则

下一个最高价值问题：**“当天完成记录”到底保存和展示什么？**

我推荐：**用完成时间过滤“今天完成”的任务，同时保留所有已完成任务在本地。**  
理由：既能满足“今天清掉了什么”的日常反馈，也不需要额外设计复杂日志系统；以后如果要做历史回顾，可以基于完成时间扩展。

请选择一个：

1. **今日过滤 + 保留已完成（推荐）**  
   任务完成时记录 `completedAt`；“当天完成记录”显示本地日期为今天的已完成任务；“已完成”里保留所有完成任务。

2. **只保留今日完成**  
   “当天完成记录”只服务今天；过了当天可以清掉或隐藏历史完成任务。更轻，但用户可能找不回昨天完成了什么。

3. **独立完成日志**  
   完成动作写入独立日志；适合未来做统计、历史回顾、撤销记录，但首版数据模型更重。

4. **已完成就是当天记录**  
   不区分“已完成”和“当天完成记录”；所有已完成任务都混在一个区里，最简单，但不满足你最初提到的“查看当天完成记录”的精确语义。

### AI user

我选 **1. 今日过滤 + 保留已完成**。

任务完成时记录完成时间；“当天完成记录”只显示本地日期为今天完成的任务，让我当天能看到自己清掉了什么，有即时反馈。

同时，已完成任务不要过夜就删除，应该继续保存在本地的“已完成”里。首版不需要做复杂历史统计、搜索或日志系统，但我希望至少昨天完成过的任务还能找回来。  
如果以后要做历史回顾，再基于 `completedAt` 扩展即可。

## Brainstorm Turn 4

### 被测 agent

已更新 PRD：确认“当天完成记录”采用 **`completedAt` 今日过滤 + 保留全部已完成任务**。

当前规则：

- 完成任务时记录 `completedAt`
- “当天完成记录”只显示本地日期为今天完成的任务
- “已完成”分组保留所有完成过的任务
- 昨天及更早完成的任务不会自动删除
- 首版不做历史统计、搜索、独立完成日志系统
- 未来如需历史回顾，可基于 `completedAt` 扩展

下一个最高价值问题：**已完成任务在首版里需要哪些管理动作？**

我推荐：**支持“恢复为未完成”和“删除”，不做批量清空。**  
理由：恢复可以修正误点完成；删除可以处理错误/垃圾任务；批量清空容易导致误删，也会和“历史已完成可找回”的目标冲突。

请选择一个：

1. **恢复 + 删除（推荐）**  
   已完成任务可以恢复为未完成，也可以单条删除；不做批量清空。

2. **只恢复，不删除**  
   最安全，避免误删；但错误任务、测试任务会一直留在已完成里。

3. **只查看，不管理**  
   最简单；但误点完成后无法修正，体验风险较高。

4. **恢复 + 删除 + 清空已完成**  
   管理能力完整，但更容易误删历史完成记录，首版复杂度也更高。

### AI user

我选 **1. 恢复 + 删除**。

已完成任务需要能单条恢复为未完成，主要是为了修正误点完成。恢复后我希望它回到“今天”，因为通常误点就是当天正在处理的任务。

也需要单条删除，用来处理输错、重复、测试或已经没有保留价值的任务。但首版不要做“清空已完成”或批量删除，避免误删历史完成记录；这个工具虽然轻量，但“已完成可找回”对我还是有价值的。

## Brainstorm Turn 5

### 被测 agent

已更新 PRD：确认已完成任务支持 **单条恢复 + 单条删除**。

当前规则：

- 已完成任务可以恢复为未完成
- 恢复后回到“今天”
- 恢复时清除完成状态和 `completedAt`
- 已完成任务可以单条删除
- 首版不做“清空已完成”或批量删除，避免误删历史完成记录

下一个最高价值问题：**未完成任务需要哪些编辑/删除能力？**

我推荐：**支持编辑内容 + 单条删除。**  
理由：快速捕捉时很容易输错或记重复；如果未完成任务不能编辑/删除，用户会被迫完成或保留错误任务，影响轻量体验。批量操作仍然不建议做。

请选择一个：

1. **编辑 + 单条删除（推荐）**  
   今天/稍后的未完成任务都可以编辑标题，也可以单条删除；不做批量删除。

2. **只删除，不编辑**  
   输错就删掉重建；实现更简单，但体验略粗糙。

3. **只编辑，不删除**  
   能修正内容，但重复/无价值任务不好清理。

4. **不做编辑/删除**  
   最小实现，但错误任务无法处理，日常使用容易卡住。

### AI user

我选 **1. 编辑 + 单条删除**。

今天和稍后的未完成任务都需要能编辑标题，因为这个工具会用于快速捕捉，输错、漏字、临时改描述都很常见。编辑只需要改任务内容本身，不需要备注、标签、优先级这些额外字段。

也需要单条删除，用来清理重复、误建或已经不值得保留的任务。首版不需要批量删除，避免增加误操作风险，也保持工具轻量。

## Brainstorm Turn 6

### 被测 agent

已更新 PRD：确认未完成任务支持 **编辑标题 + 单条删除**。

当前任务管理规则：

- 今天/稍后的未完成任务都可以编辑标题
- 编辑只改任务内容本身
- 首版不做备注、标签、优先级、项目、子任务等额外字段
- 今天/稍后的未完成任务都可以单条删除
- 首版不做批量删除

下一个最高价值问题：**跨日期之后，“今天”列表和“今日完成记录”如何变化？**

我推荐：**未完成的“今天”任务继续留在今天；今日完成记录按自然日自动变为空/新一天。**  
理由：这最符合轻量 Todo 的心智：没完成的事仍然摆在今天，不需要“逾期”概念；完成记录则按 `completedAt` 的本地日期自然过滤，到了第二天就展示新一天的完成记录。

请选择一个：

1. **今天未完成保留 + 完成记录按日过滤（推荐）**  
   过了午夜，未完成的“今天”任务仍留在“今天”；“今日完成记录”只显示新日期当天完成的任务，昨天完成的任务仍在“已完成”。

2. **今天未完成转到稍后**  
   过了午夜，没完成的今天任务自动进入“稍后”；更清爽，但有自动化规则，可能不符合你想要的手动控制。

3. **今天未完成标记为过期**  
   增加“过期/昨天遗留”状态；信息更明确，但会引入额外概念和 UI。

4. **不处理跨日期**  
   只按当前状态展示，不明确规定午夜行为；实现简单但验收和后续体验容易分歧。

### AI user

我选 **1. 今天未完成保留 + 完成记录按日过滤**。

过了午夜后，未完成的“今天”任务仍然留在“今天”。对我来说，只要还没完成，它就还是我当前需要面对的事，不需要自动挪走，也不要引入“逾期”或“昨天遗留”状态。

“今日完成记录”则按本地自然日过滤。到了第二天，它应该显示新一天已完成的任务；昨天完成的任务不再出现在“今日完成记录”里，但仍然保留在“已完成”分组中，可以找回。

## Brainstorm Turn 7

### 被测 agent

已更新 PRD：确认跨日期行为。

当前规则：

- 过了本地自然日后，未完成的“今天”任务仍留在“今天”
- “稍后”任务仍留在“稍后”
- 不引入“逾期”“昨天遗留”等状态
- 不自动把任务挪走、不自动清空今天
- “今日完成记录”按 `completedAt` 的本地自然日过滤
- 第二天的今日完成记录只显示第二天完成的任务
- 昨天完成的任务仍保留在“已完成”分组

下一个最高价值问题：**前端技术栈的实现基线怎么定？**

我推荐：**Vite + React + TypeScript。**  
理由：对 Todo 这种交互型单页应用足够轻量，同时状态、组件、测试和后续扩展都更清晰；产物仍可作为静态站点运行，不引入后端。

请选择一个：

1. **Vite + React + TypeScript（推荐）**  
   轻量 SPA，组件化清晰，适合后续测试和扩展；构建后仍是静态资源。

2. **Vanilla HTML/CSS/JS**  
   依赖最少、概念最少；但交互和测试组织会更原始，后续维护性略弱。

3. **由实现者选择轻量栈**  
   PRD 只约束“静态前端、本地存储、无后端”，具体技术由 impl 决定；灵活但实现可变性更大。

4. **其他指定技术栈**  
   你可以指定已有偏好，例如 Vue、Svelte、Solid 等。

### AI user

我选 **1. Vite + React + TypeScript**。

首版虽然是小工具，但会有分组、编辑、完成/恢复、今日过滤、本地持久化这些交互状态，用 React + TypeScript 更容易保持结构清晰，也方便后续补测试和扩展。最终产物仍然必须是静态前端资源，不引入后端、账号、数据库或同步服务。

如果实现过程中需要安装依赖或启动开发环境，请优先用 Docker / docker-compose 放在项目临时工作区里隔离，不要污染当前电脑的全局环境。

## Brainstorm Turn 8

### 被测 agent

已更新 PRD：确认技术栈和开发环境边界。

当前技术 framing：

- 使用 **Vite + React + TypeScript**
- 构建产物必须是静态前端资源
- 不引入后端 API、本地服务、SQLite、远端数据库、账号或同步服务
- 本地持久化使用浏览器本地存储，`localStorage` / `IndexedDB` 由实现按适配性选择
- 依赖安装、dev server、测试命令优先通过 **Docker / docker-compose** 在项目临时工作区隔离执行
- 不要求污染全局环境或全局安装前端依赖

接下来需要确认测试策略。这个会影响 PRD 的验收和 slice 规划。

我推荐：**核心路径测试**。  
理由：这个工具虽轻量，但状态流转比较多；至少应覆盖新增、移动、完成、恢复、删除、本地持久化、今日过滤这些核心逻辑。没必要 TDD 全覆盖，但也不建议只做手工 happy path。

请选择一个：

1. **核心路径测试（推荐）**  
   覆盖任务状态逻辑、本地持久化、今日过滤、关键 UI 流程；外部依赖无，浏览器存储可 mock/fake。

2. **TDD 驱动**  
   先写测试再实现，覆盖更完整；质量更稳，但首版节奏更慢。

3. **最小验收**  
   只做 happy path 和手工验证；最快，但状态边界更容易漏。

4. **实现者决定**  
   PRD 只规定必须能验收核心行为，具体测试范围由 impl 判断；灵活但评审口径更松。

### AI user

我选 **1. 核心路径测试**。

这个工具范围不大，但状态流转比较多，首版至少要覆盖新增任务、今天/稍后移动、完成、恢复、编辑、删除、本地持久化，以及“今日完成记录”按本地日期过滤这些核心行为。

不要求 TDD 或追求全覆盖，但不能只靠手工 happy path。尤其是刷新后数据不丢、跨日期后今日完成记录变化、已完成任务恢复回“今天”这些规则，需要有自动化测试兜底。

测试环境也请沿用前面的边界：依赖安装、测试命令和浏览器相关验证优先通过 Docker / docker-compose 在项目临时工作区里执行，不要要求全局安装前端工具。

## Brainstorm Turn 9

### 被测 agent

已更新 PRD：测试策略确认为 **核心路径测试**。

当前测试要求：

- 自动化覆盖：
  - 新增任务
  - 今天/稍后移动
  - 完成
  - 恢复到今天
  - 编辑
  - 单条删除
  - 本地持久化
  - “今日完成记录”按本地日期过滤
- 重点兜底：
  - 刷新后数据不丢
  - 跨日期后今日完成记录变化
  - 已完成任务恢复回“今天”
- 不要求 TDD 或全覆盖
- 不能只靠手工 happy path
- 依赖安装、测试命令、浏览器相关验证优先通过 Docker / docker-compose 隔离执行

现在核心范围已经比较清楚了。进入定稿前按 brainstorm 流程做一次**扩展扫视**，确认几个相邻能力是否纳入首版。

我的推荐：**只纳入“空态/基础错误态”，其余都明确 out of scope。**  
理由：空态和本地存储异常会直接影响首版可用性；导入/导出、快捷键、主题等可以后续做，不应打断轻量首版。

请选择一个：

1. **只加空态/基础错误态（推荐）**  
   增加空列表提示、存储读取/写入失败时的轻量错误提示；导入导出、快捷键、主题都不做。

2. **再加键盘快捷键**  
   支持 Enter 快速新增、可能支持 Esc 取消编辑；提升效率，但需要更多交互细节和测试。

3. **再加导出/导入 JSON**  
   方便备份本地数据；但会引入文件处理、格式兼容、错误处理，首版复杂度上升。

4. **都不加**  
   严格只做核心 Todo 流程；最快，但空态/存储错误体验可能较粗糙。

### AI user

我选 **1. 只加空态/基础错误态**。

首版需要有基本空态提示，比如“今天没有任务”“稍后没有任务”“今天还没有完成任务”，否则刚开始使用或清空列表后会显得像页面坏了。

也需要处理浏览器本地存储读取或写入失败的轻量错误提示。这个工具的数据只保存在本地，如果保存失败，用户至少要知道当前更改可能没有被保留。提示不需要复杂恢复流程，能明确告知“保存失败，请检查浏览器存储权限或稍后重试”即可。

导入/导出 JSON、键盘快捷键、主题切换都先不纳入首版。它们都有价值，但不是这个轻量 Todo 首版的核心目标，后续再根据使用反馈决定。

## Brainstorm Turn 10

### 被测 agent

已更新 PRD：扩展扫视结果已收敛。

纳入首版：

- 今天空列表提示，例如“今天没有任务”
- 稍后空列表提示，例如“稍后没有任务”
- 今日完成记录空态，例如“今天还没有完成任务”
- 浏览器本地存储读取/写入失败时的轻量错误提示
  - 语义：明确告知当前更改可能没有保存
  - 示例：“保存失败，请检查浏览器存储权限或稍后重试”
- 不要求复杂恢复流程

明确不纳入首版：

- 导入/导出 JSON
- 键盘快捷键
- 主题切换
- 复杂存储恢复流程

现在剩下一个会影响验收和实现的小边界：**任务标题输入校验规则**。

我推荐：**空标题阻止创建/保存；重复允许；超长标题限制 120 字符。**  
理由：空标题会造成无意义任务，应该阻止；重复任务在个人 Todo 中可能是合理的，不应过度拦截；120 字符足够记录一句任务，又能避免 UI 被极端输入撑坏。

请选择一个：

1. **空阻止 + 重复允许 + 120 字限制（推荐）**  
   去掉首尾空格后为空则不创建/保存；重复标题允许；标题最多 120 字符，超出时提示或截断。

2. **只阻止空标题**  
   最简单；重复和超长都交给用户，但 UI 边界弱一些。

3. **空阻止 + 重复提醒 + 120 字限制**  
   更严格；重复时提示用户确认，但会增加交互复杂度。

4. **实现者决定**  
   PRD 只要求不能产生空任务，其他由 impl 选择；灵活但验收口径较松。

### AI user

我选 **1. 空阻止 + 重复允许 + 120 字限制**。

任务标题去掉首尾空格后如果为空，就不要创建或保存；编辑时也一样，不能把已有任务保存成空标题。提示可以简单明确，比如“任务标题不能为空”。

重复标题允许。个人 Todo 里确实可能出现相同或相近的任务，比如每天都要做的检查项，不希望首版引入重复确认流程。

标题最多 **120 字符**。我更倾向于超出时阻止继续输入或保存并提示，而不是静默截断，避免用户以为内容完整保存了。提示可以是“任务标题最多 120 字”。

## Brainstorm Turn 11

### 被测 agent

已更新 PRD，并完成了一次收尾整理和自检；`sdd-arbor validate 05-05-local-first-todo-web-app` 通过。

这次确认的最后一项已写入：

- 空标题：trim 后为空则不能创建/保存，提示“任务标题不能为空”
- 重复标题：允许，不做确认流程
- 标题长度：最多 120 字符
- 超长处理：阻止继续输入或阻止保存并提示“任务标题最多 120 字”
- 不允许静默截断

PRD 现在已经整理为可执行版本，包含：

- 正式 Requirements / Acceptance Criteria
- In scope / Out of scope
- 关键用户场景与边界场景
- Technical Framing
- 数据/状态规则
- 核心路径测试策略
- 6 个有序实现 slices
- 验证重点和风险
- 无 blocking open questions

请确认是否将这个 PRD 定稿为 ready package？

如果确认，我会调用：

```bash
sdd-arbor finalize-brainstorm --input-json '{"name":"05-05-local-first-todo-web-app","kind":"single","prd_path":".arbor/tasks/05-05-local-first-todo-web-app/prd.md"}'
```

### AI user

确认，将这个 PRD 定稿为 **ready package**。

当前范围、验收标准、技术边界、状态规则、测试策略和实现 slices 都已经清楚；也没有 blocking open questions。可以调用 `finalize-brainstorm` 进入后续实现流程。

## Brainstorm Turn 12

### 被测 agent

已定稿为 ready package：

`05-05-local-first-todo-web-app`

PRD 路径：

`.arbor/tasks/05-05-local-first-todo-web-app/prd.md`

下一步进入实现时，可以使用：

```text
/sdd-kit:impl 05-05-local-first-todo-web-app
```

或直接让我用 impl skill 执行这个 package。

### AI user

请直接用 impl skill 执行这个 package：

`/sdd-kit:impl 05-05-local-first-todo-web-app`

实现时以 `.arbor/tasks/05-05-local-first-todo-web-app/prd.md` 为准，不要扩大首版范围。依赖安装、dev server、测试和浏览器验证都请优先通过 Docker / docker-compose 在项目临时工作区里隔离执行，不要污染当前电脑的全局环境。
