# Scenario dialogue — todo-greenfield-small

- 时间：2026-05-05T10:28:00.581134+00:00
- Profile：greenfield-small
- Model：claude-opus-4-6
- AI user model：claude-opus-4-6


## 初始用户需求

我想从零做一个轻量的个人任务看板，用来快速记录待办、按“今天/本周/以后”分组、支持拖拽调整优先级，并能在本地浏览器里保存数据。

## Brainstorm Turn 1

### 被测 agent

我已创建并初始化 draft PRD：`.arbor/tasks/05-05-personal-task-board/prd.md`，并把你的初始需求记录为 `grill-me` 访谈的起点。

当前理解：这是一个从零开始的轻量个人任务看板，核心是快速新增待办、按“今天 / 本周 / 以后”分组、拖拽调整优先级，并在浏览器本地持久化。真正阻塞定稿的第一个问题是产品形态，因为它会决定技术栈、持久化方式、测试范围和交付边界。

**问题 1：本次个人任务看板的产品形态应优先做成哪一种？**

我的推荐：**纯静态应用**。  
理由：无后端、无账号，打开本地页面或静态托管即可用，最符合“轻量”和“本地浏览器保存数据”。

可选方向：

1. **纯静态应用（推荐）**：无后端、无账号，浏览器内完成全部功能和本地保存。
2. **PWA 离线应用**：仍无后端，但增加安装到桌面、离线缓存等能力，体验更完整，scope 稍大。
3. **本地开发应用**：例如 Vite/React 项目，通过 `npm run dev` 使用，工程化更强，但用户使用要启动命令。
4. **带后端应用**：引入 API/服务端，为同步、多端扩展铺路，但会显著偏离轻量本地工具。

你选哪一个？

### AI user

选 **纯静态应用**。

我希望它就是一个轻量的个人工具：打开页面就能用，不需要账号、不需要服务端、不需要安装，也不需要用户理解开发命令。本地浏览器保存数据就够了。PWA 可以以后再考虑，但这次不要把安装、离线缓存、多端同步放进首版范围。

## Brainstorm Turn 2

### 被测 agent

已更新 PRD draft：确认首版是**纯静态浏览器应用**，并把 PWA、安装体验、离线缓存、多端同步、账号和服务端明确移出首版范围。

下一个最高价值问题是任务数据模型，因为它会直接决定 UI 密度、录入速度、本地存储结构和验收标准。

**问题 2：首版任务卡片需要哪些字段？**

我的推荐：**标题 + 完成状态（推荐）**。  
理由：仍然足够轻量，但“完成/未完成”是个人待办看板的核心闭环；备注、截止日期、标签会显著增加输入成本和 UI 复杂度。

可选方向：

1. **标题 + 完成状态（推荐）**：任务有标题、是否完成；保持快速录入，也能完成任务闭环。
2. **只有标题**：极简便签式看板；最快，但无法标记完成，只能删除或移动。
3. **标题 + 备注**：允许补充说明；适合稍复杂任务，但卡片和编辑交互会变重。
4. **标题 + 完成 + 截止日期**：更像传统 todo；功能更完整，但和“今天/本周/以后”分组可能产生重复语义。

你选哪一个？

### AI user

选 **标题 + 完成状态**。

首版任务应该能非常快地录入，标题就是主要内容；同时我需要一个明确的完成闭环，可以把任务标记为完成，而不是只能删除。备注、截止日期、标签这次都先不要做，避免卡片变重。

## Brainstorm Turn 3

### 被测 agent

已更新 PRD draft：任务字段收敛为 **标题 + 完成状态**，并记录首版不做备注、截止日期、标签。为了支持分组和排序，内部数据仍会有 `id / group / order / createdAt / updatedAt` 这类必要元数据，但用户可见的卡片保持轻量。

下一个最高价值问题是**拖拽语义**，因为它决定“今天 / 本周 / 以后”到底只是视图分组，还是任务状态的一部分。

**问题 3：拖拽应该只调整同组内优先级，还是也允许跨组移动任务？**

我的推荐：**允许跨组拖拽（推荐）**。  
理由：你已经用“今天 / 本周 / 以后”表达任务时段，把任务从“本周”拖到“今天”是个人看板里很自然的计划调整；如果只允许同组排序，移动分组还需要额外按钮或菜单，反而不轻量。

可选方向：

1. **允许跨组拖拽（推荐）**：同组内拖拽调整优先级，跨组拖拽改变任务分组并放到目标位置。
2. **只允许同组排序**：拖拽只改变优先级；移动分组需要另一个交互，范围更窄但使用不顺。
3. **不做真实拖拽**：用上移/下移/移动到分组按钮代替；实现更简单，但偏离你的初始需求。

你选哪一个？

### AI user

选 **允许跨组拖拽**。

“今天 / 本周 / 以后”对我来说不是单纯的展示分类，而是任务当前计划时段的一部分。用户应该可以把任务从“以后”拖到“本周”，或者从“本周”拖到“今天”，并且拖到目标组里的具体位置就代表新的优先级。

同组内拖拽调整优先级，跨组拖拽同时改变分组和优先级，这样最符合轻量看板的使用直觉。

## Brainstorm Turn 4

### 被测 agent

已更新 PRD draft：确认 **同组拖拽调整优先级，跨组拖拽同时改变计划时段和目标组内优先级**。`group` 现在是任务计划时段，`order` 是组内位置；跨组拖拽会同时更新两者。

下一个最高价值问题是**本地持久化方式**，因为它会影响实现复杂度、数据迁移和浏览器兼容边界。

**问题 4：首版本地保存用哪种浏览器存储？**

我的推荐：**localStorage + schema version（推荐）**。  
理由：首版数据很轻，任务字段少，localStorage 足够；加一个简单 `schemaVersion` 可以给未来从 localStorage 迁移到 IndexedDB 或扩展字段留出口，但不增加太多复杂度。

可选方向：

1. **localStorage + schema version（推荐）**：简单可靠，适合轻量任务数据；保留未来迁移余地。
2. **纯 localStorage**：最简单，但未来字段或结构变化时迁移边界不清楚。
3. **IndexedDB**：更适合大量数据、复杂查询和长期扩展，但首版明显偏重。
4. **导入/导出 JSON 文件**：用户可手动备份，但交互和验收会增加；可作为未来扩展。

你选哪一个？

### AI user

选 **localStorage + schema version**。

首版任务数据很轻，用 localStorage 足够；但我希望数据结构不要完全写死，至少有一个简单的 `schemaVersion`，以后如果增加字段、迁移到 IndexedDB，或者做导入导出时有明确升级路径。

首版不需要 IndexedDB，也不需要做 JSON 导入/导出备份；只要刷新页面、关闭再打开浏览器后任务、分组、排序和完成状态都能保留即可。

## Brainstorm Turn 5

### 被测 agent

已更新 PRD draft：持久化方案确认用 **localStorage + `schemaVersion`**。首版不做 IndexedDB、不做 JSON 导入/导出；验收会覆盖刷新、关闭再打开后任务标题、分组、排序、完成状态都能保留。

在进入收尾前，需要做一次扩展扫视，避免首版漏掉会影响边界的相邻能力。

**问题 5：下面这些相邻能力，哪些要进入首版？**

我的推荐：**都不进首版（推荐）**。  
理由：你前面多次强调轻量、快速录入、卡片不变重；这些能力都可以以后加，但首版如果纳入会扩大 UI、数据模型和测试范围。

可选方向：

1. **都不进首版（推荐）**：只保留新增、完成、拖拽分组/排序、本地保存。
2. **加入删除任务**：允许移除不再需要的任务；会增加一个基础操作和确认/误删边界。
3. **加入清理已完成**：批量清除完成任务；适合完成闭环，但会增加批量操作边界。
4. **加入编辑标题**：允许修正录入错误；实用但需要编辑交互和空标题校验。

你可以选一个，也可以说“加入删除 + 编辑”这类组合。

### AI user

选 **加入删除任务 + 编辑标题**。

理由是这两个属于个人 todo 首版的基本可用性，不算扩展能力：用户很容易录错标题，也会有任务不再需要；如果不能编辑或删除，工具会很快变脏，影响持续使用。

边界可以保持轻量：删除不需要复杂确认弹窗，最多用简单确认或可直接删除；编辑标题只需要支持修改已有标题，并且不允许保存空标题。**清理已完成**这次先不要做，批量操作可以以后再加。

## Brainstorm Turn 6

### 被测 agent

已更新 PRD draft：首版加入 **编辑标题** 和 **删除任务**；编辑标题必须拒绝空标题；删除保持轻量，不要求复杂确认弹窗；**批量清理已完成**明确移出首版范围。

现在主要 blocking 项只剩测试策略。这个决定会影响 PRD 的 slices 和后续 impl/review 的验收强度。

**问题 6：首版采用哪档测试策略？**

我的推荐：**核心路径测试（推荐）**。  
理由：这个工具虽轻量，但本地持久化、拖拽重排、跨组移动、空标题校验都是容易回归的核心逻辑；只做最小验收风险偏高，TDD 又可能对首版过重。

可选方向：

1. **核心路径测试（推荐）**：覆盖任务 CRUD、完成状态、同组/跨组拖拽后的数据更新、localStorage 恢复和空标题校验。
2. **最小验收**：只手动或自动跑 happy path，确认能新增、拖拽、刷新恢复；最快但回归保护弱。
3. **TDD 驱动**：先写测试再实现，覆盖所有业务逻辑层和边界；质量最高但首版成本更高。

你选哪一个？

### AI user

选 **核心路径测试**。

这个首版虽然小，但核心价值正好集中在几个容易坏的地方：任务新增/编辑/删除、完成状态、拖拽排序、跨组移动、本地保存恢复、空标题拒绝。只做 happy path 我不放心，尤其是 localStorage 和拖拽后的顺序恢复一旦出问题，用户会直接失去信任。

不要求完整 TDD，也不需要为了测试把首版工程做得很重；但 PRD 里应该明确这些核心路径必须有自动化或可重复验证的测试覆盖。

如果后续涉及开发/测试环境搭建，我的组织约束是优先用 Docker / docker-compose 放在项目临时工作区内隔离运行，不要要求污染本机全局环境。

## Brainstorm Turn 7

### 被测 agent

已更新并整理 PRD draft：测试策略确认采用 **核心路径测试**，并记录你的环境约束：如果后续涉及开发/测试环境搭建，优先用 Docker / docker-compose 放在项目临时工作区内隔离运行，不污染本机全局环境。

我也完成了收尾整理和自检：PRD 已从 evolving 内容整理到正式结构，`Open Questions` 无 blocking 项，扩展扫视已归入 scope / out of scope，Slices 已按依赖顺序写好。

最终 PRD 摘要如下：

- **产品形态**：纯静态浏览器应用，打开页面即用；无账号、无服务端、无安装、无 PWA。
- **核心功能**：
  - “今天 / 本周 / 以后”三分组。
  - 新增任务标题。
  - 编辑标题，拒绝空标题。
  - 删除单个任务。
  - 完成 / 未完成切换。
  - 同组拖拽调整优先级。
  - 跨组拖拽同时改变计划时段和目标组内优先级。
- **数据持久化**：localStorage envelope，包含 `schemaVersion: 1` 和 `tasks`。
- **Task 字段**：`id / title / completed / group / order / createdAt / updatedAt`。
- **明确不做**：账号、服务端、云同步、PWA、离线缓存、多端同步、IndexedDB、JSON 导入/导出、备注、截止日期、标签、批量清理已完成。
- **测试策略**：核心路径测试，覆盖新增、编辑、删除、完成状态、同组排序、跨组移动、本地保存恢复、空标题拒绝。
- **实现切片**：
  1. 静态看板骨架与三分组空态
  2. 任务数据模型与 localStorage 持久化
  3. 任务新增、编辑与空标题校验
  4. 完成状态与删除任务
  5. 同组拖拽排序
  6. 跨组拖拽移动计划时段
  7. 核心验收与隔离测试运行

请确认：是否按这个 PRD 定稿为 ready package？确认后我会调用 `sdd-arbor finalize-brainstorm`，然后停在 impl 前。

### AI user

确认，按这个 PRD 定稿为 **ready package**。

这个范围符合我对首版的预期：轻量、纯静态、无账号无后端，核心围绕个人任务的快速录入、分组计划、排序、完成闭环和本地保存。当前摘要里没有 blocking/open question，也没有把 PWA、同步、导入导出、备注、截止日期、标签、批量清理这类扩展能力带进首版。

可以调用 `sdd-arbor finalize-brainstorm`，并停在 impl 前。

## Brainstorm 完成

[break reason: task_state_ready]

已定稿为 ready package：`05-05-personal-task-board`

PRD 路径：

`.arbor/tasks/05-05-personal-task-board/prd.md`

已按你的确认调用：

```bash
sdd-arbor finalize-brainstorm --input-json '{"name":"05-05-personal-task-board","kind":"single","prd_path":".arbor/tasks/05-05-personal-task-board/prd.md"}'
```

我会停在 impl 前。下一步如果要开始实现，用：

```text
/ sdd-kit:impl 05-05-personal-task-board
```

或直接说“用 impl 实现 05-05-personal-task-board”。

## 进入 Impl

用 impl 执行这个 package PRD：05-05-personal-task-board
